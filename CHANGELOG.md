# Changelog

## 1.0.1 - 2026-07-14

- Added `maintainer-note` for deterministic Markdown/JSON maintainer evidence, verification commands, boundaries, and next steps after the docs and packaging pass.
- Added `final-qa` for deterministic Markdown/JSON final QA evidence with changes since v0.5.0, verification commands, release risks, artifact hashes, coherence flags, and no-advice boundaries.
- Updated README first-screen commands, command metadata, generated docs, demo outputs, package metadata, and tests for v1.0.1.
- Preserved zero runtime dependencies, no workflow automation, no live data fetching, no broker connectivity, no advice surface, and no allocation recommendations.

## 1.0.0 - 2026-07-14

- Consolidated the v0.6-v0.9 public-readiness, state/history, policy profile, cross-run promotion, and thesis-ledger artifacts into a stabilized release surface.
- Added `final-handoff` for deterministic Markdown/JSON release-owner handoff notes with repository URL placeholders, verification commands, risks, and next steps.
- Updated first-screen README CTAs, docs, tests, demo outputs, package metadata, and build backend version for v1.0.0.
- Preserved zero runtime dependencies, no workflow automation, no live data fetching, no broker connectivity, no advice surface, and no allocation recommendations.

## 0.9.0 - 2026-07-14

- Added `release-compare` for comparing release manifests or artifact catalogs across metadata and artifact hash changes.
- Added `promotion-pack` for bundling public-readiness, landing page, visual receipt, command matrix, and reviewer scorecard evidence.
- Added `adoption-notes` for reusable agent workflow notes that attach generated outputs to a thesis ledger as evidence.
- Updated docs, tests, demo outputs, package metadata, and build backend version for the cross-run promotion evidence layer.
- Preserved zero runtime dependencies, no workflow automation, no live data fetching, no advice surface, and no allocation recommendations.

## 0.8.0 - 2026-07-14

- Added `policy-profile` for applying conservative, balanced, or concentrated research-only threshold profiles to static exposure artifacts.
- Added `policy-compare` for comparing two policy-profile JSON outputs across thresholds and review flags.
- Added `policy-gallery` for deterministic Markdown/JSON examples using bundled fixture cases and policy profiles.
- Updated docs, tests, demo outputs, package metadata, and build backend version for the portfolio policy profile layer.
- Preserved zero runtime dependencies, no workflow automation, no live data fetching, no advice surface, and no allocation recommendations.

## 0.7.0 - 2026-07-14

- Added `snapshot-ledger` for append-safe deterministic artifact state snapshots without timestamps.
- Added `snapshot-compare` for comparing two snapshot JSON files across added, removed, and changed artifacts.
- Added `review-queue` for prioritized follow-up questions derived from exposure, fixture-doctor, asset-health, and release-checklist artifacts.
- Updated docs, tests, demo outputs, package metadata, and build backend version for the state/history workflow.
- Preserved zero runtime dependencies, no workflow automation, no live data fetching, and no advice surface.

## 0.6.0 - 2026-07-14

- Added `public-readiness` for a deterministic Markdown/JSON first-10-minute cold-user walkthrough.
- Added `landing-page` for a no-JavaScript public `demo/index.html` linking demos, bundle, visual receipt, command matrix, exported help, and safety boundaries.
- Added `docs-export` for deterministic Markdown/JSON CLI help and command metadata exports.
- Updated docs, tests, demo outputs, package metadata, and build backend version for the public landing/walkthrough layer.
- Preserved zero runtime dependencies, no workflow automation, no live data fetching, and no advice surface.

## 0.5.0 - 2026-07-14

- Added `artifact-catalog` for deterministic demo artifact type, byte, SHA-256, and regeneration-command inventory.
- Added `command-matrix` for Markdown/JSON CLI route coverage across inputs, outputs, and exit-code behavior.
- Added `release-checklist` for release owner checks covering tests, package, wheel smoke, public scan, no-advice boundaries, and workflow absence.
- Updated docs, tests, demo outputs, package metadata, and build backend version for the audit/reproducibility layer.
- Preserved zero runtime dependencies, no workflow automation, no live data fetching, and no advice surface.

## 0.4.0 - 2026-07-14

- Added `bundle-export` for deterministic portable demo bundles with copied artifacts and manifests.
- Added `asset-health` for operator checks covering command documentation, artifact presence, package data, and static safety boundaries.
- Added `readme-snippet` for concise Markdown quickstart/demo snippets generated from current artifacts.
- Updated docs, release notes, tests, demo outputs, and package/build versions for the operator workflow release.
- Preserved zero runtime dependencies, no workflow automation, no live data fetching, and no advice surface.

## 0.3.0 - 2026-07-14

- Added `case-gallery` for deterministic Markdown/JSON public showcase comparisons.
- Added `visual-receipt` for deterministic SVG or HTML demo artifact receipts with hashes and routes.
- Added `reviewer-scorecard` for release evidence mapped to a maturity rubric.
- Added direct-holding/ETF-wrapper example fixtures in source examples and package data.
- Updated docs, skill guidance, tests, and demo artifacts for the public showcase layer.

## 0.2.0 - 2026-07-14

- Added `fixture-doctor` for deterministic Markdown/JSON fixture quality reports.
- Added optional `source_date` and `source_url` metadata to bundled examples and package data.
- Added checks for missing funds, non-1.0 weights, duplicate fixture rows, stale source metadata, and unheld constituent funds.
- Added tests and demo artifacts for the new fixture-doctor workflow.

## 0.1.0 - 2026-07-14

- Initial public-ready package scaffold.
- Added zero-runtime-dependency CLI with static look-through calculations.
- Added bundled examples, deterministic demo artifacts, tests, and public agent skill.
